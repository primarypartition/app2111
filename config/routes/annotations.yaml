controllers:
    resource: ../../src/Controller/
    type: annotation
    prefix:
        en: '' # don't prefix URLs for English, the default locale
        pl: '/pl'
        nl: '/nl'

kernel:
    resource: ../../src/Kernel.php
    type: annotation
